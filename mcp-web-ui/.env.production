# Production environment variables.

VITE_BASE_URL = /

# Use same-origin by default. If frontend and backend are on different origins,
# override this value in your private build environment.
VITE_API_URL = /

VITE_WITH_CREDENTIALS = true

VITE_DROP_CONSOLE = true
